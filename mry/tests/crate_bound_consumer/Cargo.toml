[package]
name = "mry_crate_bound_consumer"
version = "0.5.0"
edition = "2021"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
mry = { path = "../.." }
mry_crate_bound = { path = "../crate_bound" }
